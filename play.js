//Score
const bar = document.getElementById("boy");

function jump(){
    if(jumpingBoy.classlist != "jump"){
        setTimeout(funtion() {
            boy.classlist.remove("jump");
        }, 300);
    }
}